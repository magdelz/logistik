# LogiTrack - Логистическая платформа

Современная веб-платформа для управления грузоперевозками с полной функциональностью, базой данных Supabase, админ-панелью и анимированным UI.

## 🚀 Технологический стек

### Frontend
- **Next.js 14** (App Router)
- **TypeScript**
- **Tailwind CSS**
- **Framer Motion** (анимации)
- **React Hook Form + Zod** (формы и валидация)
- **TanStack Query** (управление данными)
- **Zustand** (state management)
- **Recharts** (графики)
- **Lucide React** (иконки)

### Backend / BaaS
- **Supabase** (PostgreSQL)
- **Supabase Auth** (аутентификация)
- **Row Level Security** (RLS)

## 📦 Установка и запуск

### 1. Клонирование и установка зависимостей

```bash
cd logistics-platform
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env.local` на основе `.env.example`:

```bash
cp .env.example .env.local
```

Заполните переменные:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### 3. Настройка базы данных Supabase

1. Создайте проект на [supabase.com](https://supabase.com)
2. Перейдите в SQL Editor
3. Выполните миграцию из файла `supabase/migrations/001_initial_schema.sql`

### 4. Запуск в режиме разработки

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 🗂 Структура проекта

```
src/
├── app/                    # Next.js App Router
│   ├── (auth)/            # Страницы авторизации
│   │   ├── login/
│   │   └── register/
│   ├── (dashboard)/       # Клиентский портал
│   │   └── dashboard/
│   ├── admin/             # Админ-панель
│   ├── calculator/        # Калькулятор стоимости
│   ├── tracking/          # Отслеживание грузов
│   └── page.tsx           # Главная страница
├── components/
│   ├── animations/        # Компоненты анимаций
│   ├── layout/            # Layout компоненты
│   └── ui/                # UI компоненты (shadcn/ui)
├── hooks/                 # React hooks
├── lib/
│   ├── api/               # API функции
│   ├── supabase/          # Supabase клиенты
│   ├── validations/       # Zod схемы
│   └── utils.ts           # Утилиты
├── store/                 # Zustand stores
└── types/                 # TypeScript типы
```

## 👥 Роли пользователей

### Администратор (admin)
- Полный доступ ко всем данным
- Управление пользователями
- Управление заказами, маршрутами, тарифами
- Аналитика и отчёты

### Менеджер (manager)
- Создание и редактирование заказов
- Изменение статусов доставок
- Работа с клиентами

### Клиент (client)
- Регистрация / логин
- Создание заявки на доставку
- Отслеживание статуса груза
- История заказов

## 📄 Страницы

### Публичные
- `/` - Главная страница
- `/calculator` - Калькулятор стоимости доставки
- `/tracking` - Отслеживание груза
- `/login` - Вход в систему
- `/register` - Регистрация

### Клиентский портал (`/dashboard`)
- `/dashboard` - Обзор
- `/dashboard/orders` - Список заказов
- `/dashboard/orders/new` - Создание заказа
- `/dashboard/profile` - Профиль

### Админ-панель (`/admin`)
- `/admin` - Dashboard с аналитикой
- `/admin/orders` - Управление заказами
- `/admin/clients` - Управление клиентами
- `/admin/routes` - Управление маршрутами
- `/admin/tariffs` - Управление тарифами
- `/admin/analytics` - Аналитика

## 🗄 База данных

### Основные таблицы
- `profiles` - Профили пользователей
- `addresses` - Адреса
- `orders` - Заказы на доставку
- `routes` - Маршруты
- `tariffs` - Тарифы
- `order_status_history` - История статусов
- `activity_logs` - Логи действий
- `notifications` - Уведомления

### RLS политики
Все таблицы защищены Row Level Security:
- Клиенты видят только свои данные
- Менеджеры могут управлять заказами
- Администраторы имеют полный доступ

## 🎨 UI/UX особенности

- Современный SaaS-дизайн
- Dark/Light режимы
- Анимации Framer Motion
- Skeleton loaders
- Адаптивный дизайн
- Компоненты на базе shadcn/ui

## 🧪 Тестовые данные

Для тестирования отслеживания используйте трек-номер: `TEST123456`

## 📝 Команды

```bash
npm run dev      # Режим разработки
npm run build    # Сборка для продакшена
npm run start    # Запуск продакшен сборки
npm run lint     # Проверка линтером
```

## 🔐 Безопасность

- Аутентификация через Supabase Auth
- JWT токены для сессий
- RLS политики на уровне БД
- Middleware для защиты маршрутов
- Валидация данных через Zod

## 📧 Контакты

Для вопросов и предложений: info@logitrack.ru
